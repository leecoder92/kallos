/* eslint-disable */

// 로딩 화면 완전 확정되면 지울 페이지!
import React, { useState, useEffect } from "react";
import LoadingInterface from "@/components/LoadingInterface";

const Loading = () => {
  return <LoadingInterface />;
};

export default Loading;
